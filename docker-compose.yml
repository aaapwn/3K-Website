services:
  postgres:
    image: postgres:latest
    restart: always
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: public
      POSTGRES_USER: threekings
      POSTGRES_PASSWORD: thisis3k1ngs@w3bs1te
    volumes:
      - ./.docker-volumes/postgres:/var/lib/postgresql/data

volumes:
  postgres_data:
